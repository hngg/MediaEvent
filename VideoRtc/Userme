编译：
Makefile 生成x86的测试程序
Makefile_ios 生成iOS的.a文件，命令行下运行source runiphone.sh

命令行下运行source runbuild.bat, Android.mk 生成android的.so文件
命令行下运行source runclean.bat, 清理生成android的.so文件

注意：
Application.mk文件中可以调节APP_PLATFORM=android-10的级别兼容一些编译环境


x68调试：
运行前先make clean

test:
./SEND 720vid.h264(以文件的后缀名辨别h264文件或h265文件）
./RECV 10	(10是h264，20是h265，生成文件保存为save.h264）

./COMED_SEND *jpg
./COMED_RECV 1

